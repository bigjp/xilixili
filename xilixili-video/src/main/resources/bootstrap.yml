server:
  port: 8082
spring:
  application:
    name: xilixili-video
  cloud:
    config:
      name: xilixili-config
      profile: dev
      discovery:
        service-id: config-server
        enabled: true
  redis:
    host: 127.0.0.1
    port: 6379
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka


server:
  port: 8081
spring:
  application:
    name: xilixili-index
  cloud:
    config:
      name: xilixili-config
      profile: dev
      discovery:
        service-id: config-server
        enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka